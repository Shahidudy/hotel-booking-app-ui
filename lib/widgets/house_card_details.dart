import 'package:flutter/material.dart';

class HouseCardDetails extends StatelessWidget {
  const HouseCardDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}